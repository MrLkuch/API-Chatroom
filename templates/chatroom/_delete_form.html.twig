<form method="post" action="{{ path('app_chatroom_delete', {'id': chatroom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chatroom.id) }}">
    <button class="btn">Delete</button>
</form>
