{% extends 'base.html.twig' %}

{% block title %}Chatroom{% endblock %}

{% block body %}
    <h1>Chatroom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chatroom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chatroom.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chatroom_index') }}">back to list</a>

    <a href="{{ path('app_chatroom_edit', {'id': chatroom.id}) }}">edit</a>

    {{ include('chatroom/_delete_form.html.twig') }}
{% endblock %}
