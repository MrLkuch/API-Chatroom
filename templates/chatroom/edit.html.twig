{% extends 'base.html.twig' %}

{% block title %}Edit Chatroom{% endblock %}

{% block body %}
    <h1>Edit Chatroom</h1>

    {{ include('chatroom/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_chatroom_index') }}">back to list</a>

    {{ include('chatroom/_delete_form.html.twig') }}
{% endblock %}
