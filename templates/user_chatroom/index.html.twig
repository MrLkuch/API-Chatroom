{% extends 'base.html.twig' %}

{% block title %}UserChatroom index{% endblock %}

{% block body %}
    <h1>UserChatroom index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>LastRead</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_chatroom in user_chatrooms %}
            <tr>
                <td>{{ user_chatroom.id }}</td>
                <td>{{ user_chatroom.lastRead ? user_chatroom.lastRead|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_user_chatroom_show', {'id': user_chatroom.id}) }}">show</a>
                    <a href="{{ path('app_user_chatroom_edit', {'id': user_chatroom.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_user_chatroom_new') }}">Create new</a>
{% endblock %}
